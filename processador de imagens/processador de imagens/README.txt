Todos os arquivos desse diretório funcionam juntos.

Para rodar a interface basta executar 'main.py'

Os arquivos de imagem são caminhos fixos utilizados como referência para funcionamento geral do programa.

A interface chama o processador do octave em segundo plano, então é necessário que o computador possa encontrar o local do arquivo 'octave-cli.exe'.